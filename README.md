POLYMORPHISM USING AN ABSTRACT CLASS

In the program ,an abstract class is created by importing abc module.The class Shape inherits from the ABC class.A decorator is used before the area function in order to qualify the class as an abstract class.
The area function must be overidden in the sub-classes.The class Shape cannot be instantiated.The sub-classes are instantiated and then the area function is invoked using the objects.
